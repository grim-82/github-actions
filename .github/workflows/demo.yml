name: Demo Workfow
on: workflow_dispatch
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print to  Console
        run: echo Hello GHA
      - name: Print a few lines
        run: |
          echo First line!
          echo Second line!
